[global]
        workgroup     = WORKGROUP
        netbios name  = ubicom32
        server string = D-Link DIR-657
        security      = SHARE
        load printers = no
        guest ok      = yes

include = /etc/samba/smb.def.conf
include = /tmp/smb.dir.conf
